# Project maintenance notes

This is a standalone portfolio codebase.

- Keep the existing TanStack Start + Vite architecture.
- Keep styling and route structure stable unless a deliberate portfolio change requires it.
- Do not add platform-specific branding or telemetry.
- Keep secrets out of source control and browser-exposed environment variables.
- Run `npm run lint` and `npm run build` before publishing changes.
